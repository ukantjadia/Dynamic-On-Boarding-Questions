import 'package:flutter/material.dart';

import 'colors.dart';
final AppColors colors = AppColors(); 

ThemeData dark = ThemeData(
  fontFamily: 'Heboo',
  primaryColor: colors.primary,
  secondaryHeaderColor: colors.palceholder,
  backgroundColor: colors.bg,

);